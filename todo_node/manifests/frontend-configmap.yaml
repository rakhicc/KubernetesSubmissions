apiVersion: v1
kind: ConfigMap
metadata:
  name: todofrontend-config
  namespace: project
data:
  REACT_APP_API_BASE_URL: "/"
  REACT_APP_TODOS_PATH: "/todos"
  # ✅ image source
  REACT_APP_IMAGE_URL: "https://picsum.photos/1200"

  # ✅ cache duration (in ms)
  REACT_APP_IMAGE_CACHE_TIME: "600000"