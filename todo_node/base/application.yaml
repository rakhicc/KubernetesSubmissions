apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: project
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/rakhicc/KubernetesSubmissions.git
    targetRevision: main
    path: todo_node/manifests

  destination:
    server: https://kubernetes.default.svc
    namespace: project

  syncPolicy:
    automated:
      prune: true
      selfHeal: true