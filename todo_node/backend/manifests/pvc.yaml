apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: todo-claim # name of the volume claim, this will be used in the deployment
spec: 
  storageClassName: my-imagecache-pv
  volumeName: todo-pv
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi