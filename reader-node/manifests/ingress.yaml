apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: readwrite-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: readwrite-svc
            port: 
              number: 2345